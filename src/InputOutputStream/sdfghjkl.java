package InputOutputStream;

import java.io.*;
import java.util.Set;

public class sdfghjkl {
    public static void main(String[] args) throws Exception {
        /*InputStream i = new BufferedInputStream(new FileInputStream("/Users/subramanya.ganesh/Documents/" + "mytext.txt"));
        OutputStream out = new BufferedOutputStream(new FileOutputStream("/Users/subramanya.ganesh/Documents/" + "mytext.txt"));

        byte[] buf = new byte[1024];
        System.out.println((char) 67);
        // send '\0'
        buf[0] = 0;
        out.write(buf, 0, 1);
        out.flush();
        for (int j = 0; j < 5; j++) {
            System.out.println(i.read(buf, 0, 5));

        }
*/
       /* ObjectInputStream oin=new ObjectInputStream(new BufferedInputStream(new FileInputStream("/Users/subramanya.ganesh/log.txt")));
        Set<RelationModel> s=(Set<RelationModel>)oin.readObject();
        System.out.println(s);
*/
    }
}
